{
  "name": "@tpx/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "bun --env-file=../../apps/backend/.env run drizzle-kit generate",
    "migrate": "bun --env-file=../../apps/backend/.env run drizzle-kit migrate",
    "migrate:ci": "drizzle-kit migrate"
  },
  "dependencies": {
    "@tpx/shared": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.8.3"
  }
}
